const {
    Router,
} = require('express');
const passport = require('passport');

// required initially init function in order to run express server

const init = (app, data) => {

};

module.exports = {
    init,
};
